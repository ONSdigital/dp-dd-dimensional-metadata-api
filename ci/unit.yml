---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven

inputs:
  - name: dp-dd-dimensional-metadata-api

run:
  path: dp-dd-dimensional-metadata-api/ci/scripts/unit.sh
